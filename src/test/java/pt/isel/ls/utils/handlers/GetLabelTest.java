package pt.isel.ls.utils.handlers;


public class GetLabelTest {
    /*private final String url = "jdbc:postgresql://localhost:5432/school?user=postgres&password=1234";
    private Connection con;
    private PGSimpleDataSource ds;

    @Test
    public void getLabelTest() throws SQLException {
        ds = new PGSimpleDataSource();
        ds.setUrl(url);

        try {

            String selectQuery = "SELECT * FROM labels";
            PreparedStatement pst = con.prepareStatement(selectQuery);
            ResultSet rs = pst.executeQuery();

            assertTrue(rs.next());
        } finally {
            con.rollback();
            con.close();
        }
    }*/
}
